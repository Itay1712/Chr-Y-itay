params {
  input = "${EAGER_INPUT}"
  fasta = "${EAGER_FASTA}"
  paired_end = ${EAGER_PAIRED}
  skip_preseq = true
  skip_fastqc = true
  skip_trimming = true
  aligner = "${EAGER_ALIGNER}"
  dedupper = "${EAGER_DEDUPPER}"
  run_bam_damage = true
  outdir = "${EAGER_OUTDIR}"
}
